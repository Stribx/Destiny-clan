---
import categories from "../../data/categories.js"
---

{
    categories.map((item)=>(
        <a class="text-xl text-center border-2 rounded-lg p-4 hover:bg-sky-700" href={item.path}>{item.name}</a>
    ))
}